**/*.rs.bk
.#*
.gdb_history
target/
*.hex
*.local.toml

# mdBook build output
**/docs/book/

.DS_Store
.vscode/

# Generated TLS certificates, agent journal, and other local state
.local/
.cache/

# Claude Code local state-- only the symlinks into .agents/ are
# tracked; settings, scheduled tasks, and other session state are
# user-local and must never be committed.
.claude/*
!.claude/skills
!.claude/agents
!.claude/rules

# Pi coding harness local state
.pi/*
!.pi/rules
