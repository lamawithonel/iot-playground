**/*.rs.bk
.#*
.gdb_history
/book/*/book
target/
Cargo.lock
!test/mqtt-subscriber/Cargo.lock
*.hex
book-target/
*.local.toml

# mdBook build output
**/docs/book/

.DS_Store
.vscode/
*qemu.log
*qemuoutput.log

# Generated TLS certificates and keys
.local/
.cache/

# Claude Code local state-- only the symlinks into .agents/ are
# tracked; settings, scheduled tasks, and other session state are
# user-local and must never be committed.
.claude/*
!.claude/skills
!.claude/agents
!.claude/rules

# User-local scratch prompt files at the repo root-- never tracked.
prompt.txt
