#!/usr/bin/env bash
#MISE description="Full smoke test: covers all sensor conditioning (~13 min)"
set -o errexit
set -o nounset
set -o pipefail
export SMOKE_TEST_DURATION=780
export SAMPLE_INTERVAL_SECS=60
exec "$(dirname "$0")/smoke" "$@"
