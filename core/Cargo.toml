[package]
name = "iot-core"
version = "0.1.0"
edition = "2021"
description = "Platform-agnostic core logic for IoT firmware"

[workspace]

[dependencies]
defmt = { version = "1.0.1", optional = true }
heapless = "0.8"

[features]
default = ["defmt"]
defmt = ["dep:defmt"]
