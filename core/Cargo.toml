[package]
name = "iot-core"
version.workspace = true
edition.workspace = true
description = "Platform-agnostic core logic for IoT firmware"

[dependencies]
defmt = { workspace = true, optional = true }
heapless.workspace = true

[features]
default = ["defmt"]
defmt = ["dep:defmt"]
