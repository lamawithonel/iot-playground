[package]
name = "hal-abstractions"
version.workspace = true
edition.workspace = true
description = "Hardware abstraction traits for IoT firmware"

[features]
default = []
io = ["dep:embedded-io", "dep:embedded-io-async"]

[dependencies]
embedded-io = { workspace = true, optional = true }
embedded-io-async = { workspace = true, optional = true }
