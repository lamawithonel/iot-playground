# Do not inherit the workspace-level thumbv7em-none-eabihf
# target.  The caller (mise smoke task) passes --target with
# the detected host triple; this file only overrides runner
# settings if needed in future.
